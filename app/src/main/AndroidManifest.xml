<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AndroidERestaurant"
        tools:targetApi="31">
        <activity
            android:name=".ActivityCatDetails"
            android:exported="false"
            android:label="@string/title_activity_cat_details"
            android:theme="@style/Theme.AndroidERestaurant" />
        <activity
            android:name=".ActivityCatListe"
            android:background="@android:color/white"
            android:exported="false"
            android:label="@string/title_activity_cat_liste"
            android:theme="@style/Theme.AndroidERestaurant" />
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:label="AndroidERestaurant"
            android:theme="@style/Theme.AndroidERestaurant">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>